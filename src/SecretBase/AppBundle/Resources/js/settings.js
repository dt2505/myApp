$(document).ready(function() {
    // Initialize chosen
    // -----------------------------------------------------------------
    $('#category').chosen({width:'100%'});
    $('#country').chosen({width:'100%'});
});